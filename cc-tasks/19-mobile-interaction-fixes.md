# 手机交互修复工单（2026-09-12）

范围：仅修改 Emerald-mobile；后端和桌面只读核查，不修后端识别、不对外发布。
每项实现并验证后勾选，记录证据；真机未验证项明确保留。

- [x] 1. 自己的消息长按增加回复，沿用引用发送链路。
- [x] 2. 复制/全选不唤起输入键盘，修复焦点恢复。
- [x] 3. 全选提供可见选区和后续复制操作。
- [x] 4. 通知进入立即对账最新消息；下拉真实刷新历史，核查前后台交接与去重。
- [x] 5. 日间/夜间背景独立导入栏；所有图片导入打开相册，文件导入保留文件选择。
- [x] 6. 重启后恢复历史特殊 HTML/行内字效，保持原文与复制安全校验。
- [ ] 7. 生活记录显示本机已上传图片；压缩卡片高度、保持行距、操作右下对齐；交换类别与识别状态位置，失败红字，提高夜间对比度。
- [x] 8. 查明电脑识别失败属于上传还是后端处理，记录证据；后端问题仅报告。
- [x] 9. 思考按钮缩至文字包裹、使用系统消息级小字号，去掉思考时间/今天昨天。

依赖：1–3 共用选区交互；4/6 共用历史恢复；7/8 共用生活记录链路。5、9 可独立；本次顺序推进。

## 三面闭环与验收

逐组核查管理面设置/effective state/观测、桌面与手机能力归属、原接口鉴权/字段/关联键/去重/ack/TTL/生命周期/fallback。纯本机显示不新增后端开关。
执行相关 Flutter 测试、analyze、本地化生成及 Android 涉及部分测试/Dev 构建；独立修复通过即提交。保留现有用户改动（根 APK sha256 删除及本地化生成文件改动）。

## 施工记录

- 已创建工单，尚未修改功能代码。

- 1–3：自己的消息接入既有 ReplyTarget；菜单释放输入焦点；全选显示只读完整选区和复制按钮。交互回归 11 项通过，覆盖输入框先获焦后的回复/全选不弹键盘。仅本机 UI，无设置、权限、接口或 ack 变化。
- 4/6/9：恢复与通知打开先重读历史，再走原 mobile activation/poll；边缘拖动的零位移更新不再清空累计拖距。历史解析读取后端现有 assistant_display_text，沿用正文一致性校验和按段切片。思考按钮 9.5px、紧凑内边距、收起时移除整行底色；历史思考锚点保留日期关联但不显示时间，避免后续气泡重复今天/昨天。历史/投递 16 项、字效/选择/交互/思考 22 项回归通过。无新 HTTP 字段、scope、ack、TTL 或后台权限。
- 8：只读打开后端现有 data/runtime/life_records/records.sqlite3，jobs 分组得到 failed/ValidationError=2、failed/JSONDecodeError=1；结合 core/life_records.py 的提交后异步识别流程，确认是后端识别输出处理失败。未重试任务、未修改后端设置或代码。
- 5：日/夜背景分别预览、导入、清除；旧背景只迁移一次，清除任一槽位保留另一张和其他外观设置。所有 Android 图片导入复用原生相册入口，拍照保留相机恢复；文件/字体/JSON/PDF 仍为文件选择。双槽位通道回归、166 项全量 Flutter 测试、analyze 和 Dev APK 构建通过，未连接真机。
