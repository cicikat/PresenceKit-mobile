# 17 · 生活记录：饮食 / 账单 / 购物车

创建：2026-09-11。用户要求先工单、后施工；每项验证完成后打勾并提交。

## 范围与接力规则

手机负责用户主动拍照/选图、结构化校正、日期与分类查询、持久化离线队列及重试；电脑后端负责识别、正式数据、角色查询与权限。现有聊天上传不复用为账本，不写手机长期记忆。不自动付款或抓取其他 App。
本轮不修改后端代码；后端工单放在本仓 `cc-tasks/18-life-records-backend.md`，可直接交给后端施工者。新增 API 均为 proposed，后端未实现前保留待同步，不能伪造识别结果。

## 执行清单（按依赖顺序）

- [x] M0：核对架构、现有上传/鉴权/路由，建立工单与边界。
- [x] M1：写后端对接工单：schema、幂等、版本冲突、能力闸门、观测、角色工具与三面闭环。后端接力位于 `Emerald-presence/cc-tasks/245-life-records-backend-handoff.md`。
- [x] M2：独立模型、持久化队列和同步控制器；隔离节点/owner，失败不丢图，重试不重复入账。Android Robolectric 队列测试 14 项通过，覆盖不确定回包重试、重启、修改、删除、冲突、能力关闭与容量拒绝；另跑安全策略/凭据迁移回归。
- [ ] M3：生活记录入口、拍照/选图确认、分类和日期查询、结构化编辑/删除、状态与队列观测；中英双语。
- [ ] M4：恢复网络自动重试及生命周期接入；明确前后台/系统限制，后端未接入正确降级。
- [ ] M5：队列/同步/页面回归，静态分析与 Dev APK；更新架构、集成、风险文档，提交实现。

M1 完成后后端可独立施工；手机 M2 → M3/M4 → M5。真实后端联调和真机网络/后台验收依赖后端交付，不能提前打勾。

## 后续联调（本轮不冒充完成）

- [ ] I1：后端实现并确认 proposed 协议，用 mobile token 验证 capability/识别/查询/修改/删除/冲突/观测。
- [ ] I2：角色能按日期查询已确认记录；删除后检索不可再返回。
- [ ] I3：真机离线拍照、进程重启、网络恢复、后台/Doze、切节点/账号、重复提交验收。
- [ ] I4：淘宝官方授权直接导入调研；一期只接收用户主动提供的截图。

## 验证与遗留

施工中逐条记录测试证据；当前尚未执行实现验收。
